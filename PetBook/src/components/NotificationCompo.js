import React from "react";
import * as firebase from "firebase";
import { AuthContext } from "../providers/AuthProvider";

const NotificationCompo = (props) => {
    return(
        <text> Component </text>
    );
};

export default NotificationCompo;